＃用半小時的超時時間來重新建立您的工資計算，並創建一個帶有computepay兩個參數（小時和費率）的函數。
<法一>
input_hour =輸入（'輸入時間：'）
input_rate=input('enter rates:')
hour=float(input_hour)
rate=float(input_rate
           )
def computepay(hour,rate):
  if hour>40:
    x=(hour-40)*0.5*rate+hour*rate
    print(x)
  else:
    y=hour*rate
    print(y)

a=computepay(hour,rate)
print(a)

<法二>
hour =eval(input('輸入時間：'))
rate=eval(input('enter rates:'))

def computepay(hour,rate):
  if hour>40:
    x=(hour-40)*0.5*rate+hour*rate
    print(x)
  else:
    y=hour*rate
    print(y)

a=computepay(hour,rate)
print(a)
